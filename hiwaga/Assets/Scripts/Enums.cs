using UnityEngine;

public enum GoalType
{
    Fetch,
    Move,
    Give,
    Find
}

public enum QuestState
{
    Unaccepted,
    InProgress,
    Finished
}

public enum VolType
{
    Master,
    Music,
    SFX
}
